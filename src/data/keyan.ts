export interface Article {
  id: number;
  title: string;
  date: string;
  author: string;
  type: string;
  content: string;
}

// 科研动态：暂无上线内容（项目结题成果介绍已下线）。如需上线文章，按上方结构补充即可。
export const keyan: Article[] = [];
